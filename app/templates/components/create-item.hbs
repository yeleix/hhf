<form {{action 'createItem' on='submit'}}>
  {{input placeholder='New Item' value=newItem.content}}
  {{input type="date" placeholder='New Due Date' value=newItem.date}}
<button type='submit'>Create</button>
</form>
